using System.ComponentModel.DataAnnotations;

namespace SCA.Entity.Enums
{
    public enum PlatformType
    {
        [Display(Name = "Mobil")]
        Mobil = 1,
        [Display(Name = "Web")]
        Web = 2,
    }
}
